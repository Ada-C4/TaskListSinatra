<h2>Goddammit, you need to:</h2>
<table align="center">
  <thead>
    <tr>
      <th>
        Shit
      </th>
      <th>
        Done?
      </th>
    </tr>
  </thead>
<% @tasks.each do |task| %>
  <tr>
    <% if task[1] == nil || task[1] == "" %>
    <td> <%= task[0] %>  </td>
    <% else %>
    <td><%= task[0] %>: <%= task[1] %></td>
    <% end %>
    <td>Button that completes</td>
  </tr>
  <% end %>
</table>

<a href="/add_task" class="button">got more shit to do?</a>
